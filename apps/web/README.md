# Apps — Web

Browser-based marketing tools built with HTML, React, or Next.js.

---

## Running a web app

Most apps open directly in a browser — no setup needed.
Check each app's own README for specific instructions.
Some may require `npm install` if they use Node.js.

---

## Browse apps

| Slug | Framework | Summary | Requires API |
|---|---|---|---|
| *(coming soon)* | | | |

---

## Structure of each app

```
[app-slug]/
├── index.html       (or src/, pages/, etc. depending on framework)
├── .env.example     ← if API calls needed
├── metadata.yaml
├── README.md
└── assets/
```
